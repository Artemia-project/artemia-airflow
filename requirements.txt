# Airflow ETL Pipeline Dependencies (Essential Only)
# 기본 Airflow 패키지
apache-airflow==3.0.3
apache-airflow-providers-microsoft-mssql==3.5.0
apache-airflow-providers-http==4.7.0
apache-airflow-providers-github==2.5.0

# 데이터베이스 연결
pymssql==2.2.11

# HTTP 요청
requests==2.31.0

# 환경 설정 및 날짜 처리
python-dotenv==1.0.0
python-dateutil==2.8.2
pytz==2023.3